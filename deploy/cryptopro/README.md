# CryptoPro packages

Place the licensed CryptoPro Linux `.deb` packages required by the Dockerfile
in this directory before building the runtime image.

The packages are proprietary and intentionally excluded from version control.
Use packages obtained under your own licence and verify their checksums.
