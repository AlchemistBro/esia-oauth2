# Security policy

Report suspected vulnerabilities through GitHub private security advisories.
Do not publish credentials, tokens, certificates, private keys, CryptoPro
container data or production URLs in an issue.

This repository intentionally contains no deployment credentials. Operators
must protect key containers, rotate secrets, use explicitly trusted ESIA
certificates and place the callback service behind a TLS reverse proxy.
